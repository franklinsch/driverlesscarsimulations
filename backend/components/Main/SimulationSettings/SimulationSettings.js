import React from 'react';
import Dropdown from './Dropdown/Dropdown.js'

export default class SimulationSettings extends React.Component {
  static propTypes = {
    handleCityChange: React.PropTypes.function
  }

  render() {
  }
}
