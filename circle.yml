machine:
  timezone: Europe/London

dependencies:
  override:
    - cd webserver
    - npm install

test:
  override:
    - npm test
    - cd ../framework/test
    - python -m unittest client_test.py
