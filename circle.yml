machine:
  timezone: 
    Europe/London
  python:
    version: 3.5.2

dependencies:
  override:
    - cd framework && python setup.py install

test:
  override:
    - cd framework/test/ && python -m unittest client_test.py 
deployment:
  production:
    branch: capistrano
    commands:
    - ssh circleci@35.160.255.102 
    - gem install capistrano
    - cap production deploy
